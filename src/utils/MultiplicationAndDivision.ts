export function calculateMultiplicationAndDivision(number1: number, number2: number){
    const multiplication = number1 * number2;
    const division = number1 / number2;
  
    return { multiplication, division };
  }